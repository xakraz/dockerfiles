#!/usr/bin/env bash
#
#

# == Vars
#
# Hub.docker.io
DOCKER_REGISTRY=""
DOCKER_IMAGE=sofianinho/pwgen-alpine
DOCKER_IMAGE_TAG=latest

docker run \
  --rm \
  ${DOCKER_REGISTRY}${DOCKER_IMAGE}:${DOCKER_IMAGE_TAG} \
  "$@"
